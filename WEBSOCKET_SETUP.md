# WebSocket Real-time Updates Setup

## Обзор

Система теперь поддерживает обновления в реальном времени через WebSocket для тикетов и комментариев.

## Backend

### Структура

1. **WebSocket Manager** (`backend/app/infrastructure/websocket/manager.py`)
   - Управляет активными соединениями
   - Поддерживает подписки на тикеты
   - Отправляет события всем подключенным клиентам

2. **WebSocket Router** (`backend/app/presentation/api/v1/routers/websocket.py`)
   - Endpoint: `/api/v1/ws`
   - Аутентификация через JWT токен в query параметре
   - Поддержка подписки на конкретные тикеты

3. **Обновленные Endpoints** (`backend/app/presentation/api/v1/routers/tickets.py`)
   - При создании тикета отправляется событие `ticket_created`
   - При обновлении тикета отправляется событие `ticket_updated`
   - При удалении тикета отправляется событие `ticket_deleted`
   - При добавлении комментария отправляется событие `comment_added`

### События

- `ticket_created` - новый тикет создан
- `ticket_updated` - тикет обновлен
- `ticket_deleted` - тикет удален
- `comment_added` - добавлен комментарий

## Frontend

### WebSocket Service (`src/services/websocket.js`)

- Автоматическое подключение при авторизации
- Автоматическое переподключение при разрыве соединения
- Поддержка подписки на конкретные тикеты
- Heartbeat (ping/pong) для поддержания соединения

### Интеграция

1. **TicketsContext** (`src/context/TicketsContext.jsx`)
   - Автоматически подключается к WebSocket при авторизации
   - Обновляет состояние тикетов в реальном времени
   - Обрабатывает все события от WebSocket

2. **TicketDetail** (`src/pages/TicketDetail.jsx`)
   - Подписывается на обновления конкретного тикета
   - Получает обновления комментариев в реальном времени

## Использование

### Подключение

WebSocket автоматически подключается при авторизации пользователя. Токен передается через query параметр:

```
ws://localhost:8000/api/v1/ws?token=<JWT_TOKEN>
```

### Подписка на тикет

```javascript
wsService.subscribeToTicket(ticketId)
```

### Обработка событий

```javascript
wsService.on('ticket_updated', (data) => {
  console.log('Ticket updated:', data.ticket)
})
```

## Проверка работы

1. Откройте приложение в двух браузерах
2. Создайте тикет в одном браузере
3. Тикет должен появиться во втором браузере автоматически
4. Добавьте комментарий - он появится в обоих браузерах в реальном времени

## Отладка

В консоли браузера будут видны сообщения:
- `🔌 Connecting to WebSocket` - подключение
- `✅ WebSocket connected` - успешное подключение
- `❌ WebSocket error` - ошибка подключения
- `🔄 Reconnecting` - переподключение




